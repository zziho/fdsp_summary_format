# FSDP Summary Format 학습 — 작업 과정 및 결과

작성일: 2026-08-16  
작업 디렉토리: `/workspace/fdsp_summary_format`  
기준 원본: `/workspace/summary_format` (Unsloth + 단일 GPU LoRA)

---

## 1. 이 작업이 뭔지

기존 `summary_format`은 **Unsloth + 4bit + LoRA**로 Gemma 3 12B IT를 학습하는 파이프라인이다.

이번 작업의 목표는 그 **플로우는 그대로 유지**하면서, Unsloth 대신 **Transformers / TRL / PEFT + FSDP(2 GPU)** 로 다시 돌리는 것이다.

### 학습 플로우 (기존 = 이번 FSDP 버전)

```text
1) SFT (LoRA)
   base: Gemma 3 12B IT
   data: model_input_fixed.jsonl  (system + user + assistant)

2) SFT merge
   LoRA adapter → full BF16 모델

3) GRPO (LoRA)
   base: SFT merged
   data: 같은 jsonl에서 assistant를 떼고 prompt만 사용
   모델이 N개 생성 → format reward로 채점

4) GRPO merge
   GRPO LoRA adapter → 최종 full BF16 모델
```

### 데이터 의미

| 단계 | 입력 | 정답/보상 |
|------|------|-----------|
| SFT | `system + user(진료대화 + 항목명)` | `assistant` gold 요약 |
| GRPO | 같은 `system + user`만 | 모델이 직접 생성한 후보를 format reward로 채점 |

사용 데이터:  
`/workspace/summary_format/train_data/model_input_fixed.jsonl` (1,131개)

참고: 예전에 기억하시던 SFT 1,422 / GRPO 369 숫자는 이 워크스페이스에 남아 있지 않았고, **코드가 실제로 가리키는 파일은 `model_input_fixed.jsonl`** 이다.

---

## 2. FSDP가 뭔지 (이 프로젝트 기준 상세)

### 2.1 한 줄 정의

**FSDP = Fully Sharded Data Parallel**  
= 여러 GPU가 **같은 모델을 복제하지 않고**, 파라미터/그래디언트/옵티마이저 상태를 **쪼개서(shard)** 나눠 가진 뒤,  
필요한 순간에만 잠깐 모아서(all-gather) 계산하는 분산 학습 방식.

기존 Unsloth 코드는 사실상 **한 GPU에 모델(또는 4bit 압축 모델)을 올리는 방식**이었다.  
이번 `fdsp_summary_format`은 **A100 40GB × 2**에서 BF16 LoRA를 안정적으로 돌리려고 FSDP를 쓴다.

### 2.2 왜 필요했나

Gemma 3 12B를 BF16으로 대략 보면:

| 항목 | 대략 크기 |
|------|-----------|
| 모델 파라미터 (BF16) | ~24GB |
| Optimizer states (AdamW, 학습 파라미터가 전부일 때) | 매우 큼 |
| Activation / 생성 버퍼 (특히 GRPO) | 추가 수~수십 GB |

A100 40GB **한 장**에 12B BF16 full fine-tune은 빡세고,  
Unsloth처럼 **4bit + 단일 GPU**면 되지만 multi-GPU BF16 LoRA를 제대로 쓰려면  
파라미터를 GPU 사이에 나눠야 한다. 그게 FSDP다.

이번 선택은:

- **Full fine-tune이 아니라 LoRA** → 학습 파라미터는 작음
- 그래도 **base 가중치는 BF16으로 두 GPU에 shard**
- Unsloth QLoRA 대신 **표준 Transformers/TRL 경로**로 multi-GPU

### 2.3 DDP / ZeRO / Unsloth와 뭐가 다른지

```text
[단일 GPU / Unsloth QLoRA]
  GPU0: 4bit 모델 전체 + LoRA
  → 구현 단순, 빠르지만 multi-GPU FSDP와 궁합이 나쁨

[DDP (Distributed Data Parallel)]
  GPU0: 모델 전체 복제
  GPU1: 모델 전체 복제
  → 배치만 나눠서 학습. 메모리 절약이 거의 없음
  → 12B BF16이면 GPU마다 ~24GB를 또 먹음

[FSDP (Fully Sharded)]
  GPU0: 파라미터의 일부 shard
  GPU1: 파라미터의 나머지 shard
  forward 직전: 잠깐 모음(all-gather) → 계산 → 다시 쪼갬(reshard)
  → 메모리/GPU 수 trade-off. multi-GPU 대형 모델에 적합
```

DeepSpeed ZeRO-3와 아이디어는 비슷하다.  
차이점은 생태계/API: 이 프로젝트는 Hugging Face `Trainer` + Accelerate FSDP 경로를 쓴다.

### 2.4 Forward / Backward가 실제로 하는 일

layer 단위로 대략 이렇게 돈다 (`Gemma3DecoderLayer`를 wrap):

```text
1. 이 layer의 shard를 모든 GPU에서 all-gather → 잠깐 "완성된 layer 가중치" 만들기
2. forward 계산
3. (reshard_after_forward=True) 다시 shard로 쪼개서 메모리 반환
4. backward 때 다시 gather → gradient 계산
5. reduce-scatter로 gradient shard만 각자 보관
6. 각자 자기 shard에 해당하는 optimizer step
```

그래서 FSDP는 **통신(NCCL)이 필수**다.  
이 머신에서 P2P NCCL이 hang 났던 이유가 바로 이 통신 경로 문제였다.

### 2.5 FSDP1 vs FSDP2

| | FSDP1 | FSDP2 (이번 사용) |
|--|-------|-------------------|
| API | `FullyShardedDataParallel` | `fully_shard` / DTensor 기반 |
| Transformers 기본 | 구버전 | 최신 기본 (`fsdp_config.version=2`) |
| tied weight | 상대적으로 관대 | **엄격** (cross-group tie 금지) |
| 권장 activation ckpt | gradient_checkpointing | `fsdp_config.activation_checkpointing` |

이번 코드는 **FSDP2**다.  
그래서 Gemma의 `embed_tokens ↔ lm_head` tie를 풀어야 했고,  
adapter 저장도 DTensor `full_tensor()` 경로를 탔다.

### 2.6 이 레포의 FSDP 설정 해설

`common.py`의 `fsdp_config()`:

```python
{
  "version": 2,                              # FSDP2
  "auto_wrap_policy": "TRANSFORMER_BASED_WRAP",
  "transformer_layer_cls_to_wrap": ["Gemma3DecoderLayer"],
  "reshard_after_forward": True,             # forward 후 즉시 다시 shard
  "activation_checkpointing": True,          # activation 메모리 절약
  "cpu_ram_efficient_loading": True,         # rank0 로드 후 분배
  "state_dict_type": "FULL_STATE_DICT",
}
```

그리고 학습 스크립트에서:

- `fsdp=True`
- `bf16=True`
- Trainer `gradient_checkpointing=False`  
  (FSDP activation checkpointing과 중복되면 불필요한 AllGather가 생김)
- LoRA는 language tower만:
  `.*language_model.*(q_proj|k_proj|v_proj|o_proj|gate_proj|up_proj|down_proj)`
- 실행:
  ```bash
  torchrun --standalone --nproc_per_node=2 train_sft.py
  ```

### 2.7 이번 런의 메모리 그림 (실측)

Smoke / full SFT 중 관측:

- GPU0 / GPU1 각각 약 **31~32GB** 사용
- utilization 둘 다 높음 → 실제로 2장 모두 연산 참여
- step당 약 **80~83초** (SFT, seq 길이가 긴 편)

해석:
- BF16 12B를 두 장에 shard + LoRA + activation ckpt 조합
- Unsloth 4bit 단일 GPU보다 step은 느릴 수 있지만,  
  **표준 multi-GPU / BF16 / FSDP 경로**를 확보한 것이 목적

### 2.8 FSDP + LoRA에서 꼭 알아야 할 점

1. **Frozen base + trainable LoRA가 섞임**  
   FSDP가 mixed trainable params를 다루려면 최신 설정(특히 FSDP2)과 올바른 wrap이 필요
2. **저장은 조심**  
   `Trainer.save_model()`이 full state를 gather하면 12B 전체를 조립하려 해서 매우 느림/위험  
   → 이 레포는 `save_fsdp_adapter()`로 `lora_*`만 gather
3. **NCCL이 죽으면 FSDP도 죽음**  
   P2P/IB 이슈 시 `NCCL_P2P_DISABLE=1`, `NCCL_IB_DISABLE=1`
4. **Gemma 3 IT는 multimodal wrapper**  
   vision tower까지 로드되지만 LoRA는 text tower만 학습

### 2.9 이번 작업에서 FSDP로 "한 일"의 의미

- 기존: Unsloth가 내부적으로 메모리 최적화 + 단일 프로세스 학습
- 이번:  
  1) 모델을 표준 HF 형식으로 로드  
  2) LoRA 부착  
  3) `torchrun`으로 프로세스 2개 기동  
  4) Accelerate/Transformers가 FSDP2로 layer를 shard  
  5) SFT/GRPO Trainer가 분산 step 수행  
  6) LoRA만 저장 → 나중에 CPU에서 merge

즉 **학습 알고리즘(SFT/GRPO)은 같고, 모델을 GPU에 올리는 방식만 FSDP로 바꾼 것**이다.

---

## 2-B. 왜 FSDP를 썼나 / 5축 evaluation / trade-off

> “FSDP로 학습이 돌아갔다” 자체보다, **왜 FSDP를 썼고 어떤 trade-off가 생겼는지**가 핵심이다.

### 왜 FSDP를 썼나

목표는 Unsloth 4bit 단일 GPU가 아니라, **BF16 + 표준 HF/TRL 경로로 2 GPU**에서 같은 SFT→GRPO 플로우를 도는 것이었다.

FSDP의 본질:
- 파라미터 / gradient / optimizer state를 GPU에 **shard**
- 필요할 때만 all-gather로 잠깐 모음
- GPU당 상주 메모리를 줄여 큰 모델을 multi-GPU로 올림

이번 선택의 이유:
- Gemma 3 12B BF16 ≈ 24GB급 → A100 40GB 1장에 DDP 복제나 full FT는 빡셈
- Unsloth는 빠르지만 FSDP multi-GPU와 안 맞음
- 그래서 **LoRA(학습 파라미터는 작게) + FSDP(base를 2장에 shard)**

즉 FSDP는 속도 마법이 아니라 **메모리/스케일 전략**이다.

### 이번 실험에서 본 5가지

#### 1) GPU별 VRAM

실측(SFT 중):
- GPU0 ≈ **33.8GB / 40GB**
- GPU1 ≈ **33.8GB / 40GB**
- 거의 대칭 → shard가 실제로 양쪽에 분배됨

해석:
- “FSDP라서 한 장이 12GB만 쓴다” 수준은 아님
- 이유: BF16 base + activation + LoRA + FSDP gather 버퍼가 같이 있음
- 그래도 **DDP로 12B를 양쪽에 풀복제**하는 것보다는 메모리 전략이 맞고, 두 장이 비슷하게 찬 건 FSDP가 동작 중이라는 신호
- 비교 baseline(Unsloth 4bit 단일 GPU)을 같은 배치로 안 재서, “얼마나 줄었는지” **정량 A/B는 아직 약함** → 보고서에 솔직히 기록

#### 2) Throughput

대략:
- step time ≈ **75–83 s/step**
- 유효 배치: `1 × 16 × 2 = 32`
- tokens/step ≈ **4.2만–4.6만**
- tokens/sec ≈ **530–590**
- samples/sec ≈ **0.39** (smoke 때 `train_samples_per_second`)

해석:
- 돌아는 가지만 **느림**
- 긴 seq(최대 4096), grad accum 16, FSDP all-gather, NCCL P2P 비활성까지 겹침

#### 3) 통신 오버헤드 (왜 2배 안 빨라지나)

2 GPU여도 2×가 아닌 이유:
1. FSDP는 step마다 **all-gather / reduce-scatter**
2. 이 호스트는 `NCCL_P2P_DISABLE=1`, `NCCL_IB_DISABLE=1` → 느린 경로
3. LoRA여도 base shard gather 비용은 남음
4. 데이터 병렬 이득과 통신 비용이 상쇄

trade-off:
- 이득: multi-GPU BF16 경로, 메모리 shard
- 비용: step latency, 통신, 운영 복잡도

#### 4) Checkpoint

이번 실험의 중요한 발견:
- `Trainer.save_model()` FULL gather → 12B 조립으로 **사실상 실패/비실용**
- `lora_*`만 gather하는 adapter save로 우회 → **성공**
- merge는 학습 후 CPU에서 별도 수행

즉:
- “sharded checkpoint 완벽 지원”까지는 못 봄
- **실용 체크포인트 = LoRA adapter save + 사후 merge**
- resume-from-sharded-full-ckpt는 이번 범위에서 검증 약함

보고서 문장:
> FSDP full state dict는 비싸고, PEFT adapter 단위 저장이 현실적 해법이었다.

#### 5) 학습 안정성

| step | loss | acc | grad_norm |
|------|------|-----|-----------|
| 10 | 2.78 | 0.53 | 0.78 |
| 40 | 1.03 | 0.75 | 0.32 |
| 90 | 0.57 | 0.84 | 0.35 |
| 100 | 0.56 | 0.84 | 0.34 |

- loss 단조 하락
- accuracy 상승
- OOM 없음
- NCCL hang은 초기에 있었으나 env로 해결 후 재발 없음
- SFT는 108 step 중 후반까지 안정적으로 진행

### 한 줄로 정리할 trade-off

- **FSDP를 쓴 이유:** 12B BF16을 2×A100에서 표준 스택으로 올리기 위한 메모리 shard
- **얻은 것:** multi-GPU 학습 경로, 안정적인 loss 감소, LoRA 기반 실용 저장
- **잃은 것/비용:** step이 느림, 2GPU≠2×속도, NCCL 튜닝 필요, full sharded ckpt 대신 adapter 우회

---

## 3. 전체에서 한 일 (타임라인)

1. 기존 `summary_format` 구조/플로우 확인  
   - base: `unsloth/gemma-3-12b-it` (= Gemma 3 12B Instruct)  
   - SFT → merge → GRPO → merge 확인
2. `/workspace/fdsp_summary_format` 에 FSDP용 코드 신규 작성
3. 학습 의존성 설치 (`transformers`, `trl`, `peft`, `accelerate`, `datasets` 등)
4. base 모델 다운로드
5. 2-GPU FSDP smoke test (1 step)로 검증
6. 전체 SFT 학습 시작
7. (진행 중) SFT 완료 후 merge → GRPO → GRPO merge

---

## 4. 산출물 / 코드 구조

```text
fdsp_summary_format/
├── common.py              # 경로, FSDP/LoRA 설정, 데이터 로더, adapter 저장
├── train_sft.py           # Stage 1: FSDP LoRA SFT
├── merge_adapter.py       # Stage 1/2 merge
├── train_grpo.py          # Stage 2: FSDP LoRA GRPO
├── rewards.py             # format reward (json/html/md)
├── run_pipeline.sh        # 전체 파이프라인 실행 스크립트
├── test_distributed.py    # NCCL 연결 점검용
├── requirements.txt
├── README.md
└── outputs/
    ├── sft_adapter/       # SFT LoRA
    ├── sft_merged/        # SFT merge 결과
    ├── grpo_adapter/      # GRPO LoRA
    └── grpo_merged/       # 최종 모델
```

### 실행 하드웨어

- GPU: **NVIDIA A100-SXM4-40GB × 2**
- 전략: FSDP2 `full shard` + LoRA (language tower만)
- dtype: BF16
- 데이터 병렬: `torchrun --nproc_per_node=2`

### 학습 하이퍼파라미터 (요지)

**SFT**
- epochs: 3
- per_device_batch: 1
- grad_accum: 16  
  → 유효 global batch ≈ `1 × 16 × 2 = 32`
- lr: `2e-4`
- max_length: 4096
- LoRA rank/alpha: 16
- 총 step: **108**

**GRPO**
- epochs: 1
- num_generations: 4
- max_completion_length: 700
- lr: `2e-6`
- reward: format compliance (markdown / markdown table / html / html table / jsonl)

---

## 5. 문제와 해결

중간에 여러 번 막혔다. **시간 순서대로** 정리하면 아래와 같다.

```text
① google/gemma-3-12b-it 다운로드 실패 (gated)
        ↓
② unsloth/gemma-3-12b-it 로 우회 다운로드 성공
        ↓
③ torchrun 2GPU 기동은 되는데 trainer 직후 hang
   (GPU util 100%, memory ~0.7GB → 학습 미진입)
        ↓
④ test_distributed.py 로 NCCL 자체 hang 확인
        ↓
⑤ NCCL_P2P_DISABLE=1 / NCCL_IB_DISABLE=1 로 통신 복구
        ↓
⑥ 학습 step 진입 직후 FSDP2 tied embedding 에러
        ↓
⑦ lm_head untie 후 1-step 학습 성공
        ↓
⑧ trainer.save_model() 이 12B full gather로 장시간 정지
        ↓
⑨ lora_* 만 gather 하는 save_fsdp_adapter() 로 저장 정상화
        ↓
⑩ 전체 SFT 108 step 시작
```

### 5.1 Hugging Face gated 모델 접근 거부

- 시도: `google/gemma-3-12b-it`
- 결과: `Access denied. This repository requires approval.`
- 원인: Gemma 라이선스/액세스 승인 미완료 토큰
- 증상 해석: 모델 메타데이터는 보이는데 **파일 다운로드만 거절**
- 해결: 기존 파이프라인과 동일한 가중치를 제공하는  
  **`unsloth/gemma-3-12b-it`** 를 다운로드  
  저장 위치: `/workspace/models/gemma-3-12b-it` (약 23GB)

> 보안 메모: HF 토큰을 채팅에 직접 올리면 유출 위험이 있다.  
> 가능하면 해당 토큰을 revoke 후 재발급하고, 환경변수/`$WORKSPACE/.env`로만 쓰자.

### 5.2 NCCL 통신 교착 (가장 큰 블로커)

이게 제일 오래 막힌 지점이다.

- 증상:
  - `SFTTrainer` 생성/데이터 전처리까지는 됨
  - 그 다음 `accelerator.wait_for_everyone()` / `barrier()` 에서 **수분~수십 분 hang**
  - `nvidia-smi` 상 GPU util은 100%인데 **memory는 ~700MB**  
    → forward에 들어간 게 아니라 NCCL 대기/스핀 상태
- 스택 덤프(`SIGUSR1`)로 확인한 위치:
  - `torch.distributed.barrier`
  - `accelerate.state.wait_for_everyone`
  - TRL `_prepare_dataset` 직후 sync
- 재현:
  - `test_distributed.py` 로 단순 `all_reduce` + `barrier` 만 돌려도 기본 NCCL은 hang
  - `timeout 20s` 걸면 타임아웃
- 해결:
  ```bash
  export NCCL_P2P_DISABLE=1
  export NCCL_IB_DISABLE=1
  ```
  같은 테스트가 수 초 만에 성공 (`rank0/1 all_reduce=3.0`)
- 해석:  
  이 호스트의 **GPU direct P2P / IB 경로가 불안정**하고,  
  shared-memory 쪽 collectives는 정상.  
  FSDP는 step마다 all-gather를 쓰므로 NCCL이 죽으면 학습 전체가 멈춘다.
- 반영 위치: `train_sft.py`, `train_grpo.py`, `run_pipeline.sh`

### 5.3 FSDP2 + tied embedding 충돌

NCCL 고치고 나서 바로 다음 벽.

- 에러:
  ```text
  Parameter '...embed_tokens.weight' is shared with a parameter already
  managed by another FSDP group.
  ```
- 언제: `trainer.train()` 첫 forward 직전 FSDP lazy init
- 원인: Gemma가 `embed_tokens`와 `lm_head`를 tie 하는데,  
  FSDP2는 **서로 다른 shard group의 tied weight를 거부**
- 해결: 로드 직후 `lm_head.weight`를 clone해서 untie  
  (`tie_word_embeddings=False`)  
  시작 값은 동일, storage만 분리 (`common.py`의 `load_model()`)
- 참고: 기존 `summary_format` merge 코드에도 비슷한 untie 트릭이 있었음

### 5.4 FSDP config API 변경

- 증상 1: `fsdp="full_shard auto_wrap"` → deprecation / 새 API 권장
- 증상 2: `auto_wrap_policy="transformer_based_wrap"` → ValueError  
  (대문자 enum만 허용: `TRANSFORMER_BASED_WRAP`)
- 추가 경고: FSDP에서 Trainer `gradient_checkpointing`을 쓰면  
  backward에 불필요한 AllGather가 생길 수 있음
- 해결:
  - `fsdp=True`
  - `fsdp_config.version=2`
  - `auto_wrap_policy="TRANSFORMER_BASED_WRAP"`
  - `activation_checkpointing=True` (FSDP 쪽)
  - Trainer `gradient_checkpointing=False`

### 5.5 Adapter 저장이 비정상적으로 오래 걸림

학습 자체는 통과했는데 “다 된 줄 알고” 또 막힌 구간.

- 증상:
  - 1-step train 성공 (loss ≈ 2.95, ~82초)
  - 이후 `trainer.save_model()` 에서 다시 장시간 정지
  - GPU0 memory ~29GB, util 100% / GPU1은 거의 idle
- 스택 덤프 위치:
  - `torch.distributed.checkpoint...get_model_state_dict`
  - `_gather_state_dict`
  - Accelerate `get_state_dict` → Transformers `save_model`
- 원인: FSDP `FULL_STATE_DICT`가 **LoRA만이 아니라 12B 전체**를 gather
- 해결: `save_fsdp_adapter()`
  - 모든 rank가 같은 순서로 `lora_*` 파라미터만 `full_tensor()` 호출
  - rank0만 CPU에 모아 PEFT adapter로 저장
  - smoke test 기준 저장이 수 초 내로 완료

### 5.6 Gemma 3 multimodal wrapper

막힘이라기보다 구조적 주의점.

- HF의 Gemma 3 IT는 `Gemma3ForConditionalGeneration` (vision tower 포함)
- LoRA는 **language tower projection만** 타겟팅:
  ```text
  .*language_model.*(q_proj|k_proj|v_proj|o_proj|gate_proj|up_proj|down_proj)
  ```
- chat template은 `system`을 첫 `user` turn에 합치는 형태인데, 학습에는 문제 없음
- FSDP wrap 단위는 `Gemma3DecoderLayer` (텍스트 decoder)

---

## 6. 현재 진행 상태 (스냅샷)

| 단계 | 상태 | 비고 |
|------|------|------|
| 코드 작성 | ✅ 완료 | `fdsp_summary_format/` |
| 의존성 설치 | ✅ 완료 | `/venv/main` |
| 모델 다운로드 | ✅ 완료 | `/workspace/models/gemma-3-12b-it` |
| FSDP smoke test | ✅ 완료 | 1 step, loss ≈ 2.95 |
| **전체 SFT** | ✅ 완료 | 108/108 steps, ~2h19m |
| SFT merge | ✅ 완료 | FSDP key remap → PEFT load |
| GRPO | ⏹ 중단 | 2/960 steps 후 abort (~11–12분/step, ETA ~181h) |
| GRPO merge | ⏳ 미실행 | |

SFT 최종 관측:
- step당 약 **75~83초**, wall-clock **8344s (~2h19m)**
- GPU memory: 각 A100에서 약 **33.8GB**
- `train_loss≈1.044`, 마지막 구간 loss≈0.56, `mean_token_accuracy≈0.85`
- adapter: `outputs/sft_adapter` (~250MB), merged: `outputs/sft_merged` (~23GB)
- **깨끗한 PEFT 키 adapter 복사본은 별도 저장하지 않음** (원본 `sft_adapter`는 FSDP 키 그대로, merge 시 `/tmp`에서만 정규화)

Merge 이슈: 저장된 LoRA 키에 `_checkpoint_wrapped_module`과 `lora_A.weight`(`.default` 없음)가 있어 PEFT load가 깨짐 → `merge_adapter.py`가 임시 복사본에서 키를 정규화. 이후 `save_fsdp_adapter()`도 동일 정규화로 수정.

호스트 통신: `nvidia-smi topo`상 GPU0↔GPU1이 `NODE`(NVLink `NV#` 없음), 컨테이너에 `/dev/infiniband` 없음. P2P 경로는 hang → `NCCL_P2P_DISABLE=1` / `NCCL_IB_DISABLE=1`로 학습(느린 fallback).

GRPO 비고: generation(`num_generations=4`, `max_completion_length=700`, `use_vllm=False`) + FSDP all-gather 때문에 step이 SFT(~80s) 대비 ~10분대로 김. FSDP 검증 목적만이면 SFT로 충분.

---

## 7. 결과 기록란 (완료 후 채움)

### SFT

- 완료 시각: 2026-08-16 17:36 UTC
- 최종 train loss: **1.044** (마지막 로깅 구간 loss ≈ 0.56)
- adapter 경로: `outputs/sft_adapter` (~250MB, 672 LoRA tensors)
- merged 경로: `outputs/sft_merged` (~23GB, 7 shards)
- 비고: FSDP FULL_SHARD 2×A100; merge 시 checkpoint-wrapper 키 정규화 필요했음

### GRPO

- 완료 시각:
- mean reward / loss:
- adapter 경로: `outputs/grpo_adapter`
- merged 경로: `outputs/grpo_merged`
- 비고: 2/960에서 중단. step ≈ 680–700s, ETA ≈ 181h. full GRPO는 미완료.

### 최종 산출물

- 최종 모델:
- 간단 추론 샘플:
- 느낀 점 / 다음 액션:

---

## 7-B. 학습 메트릭 (중간/최종 확인용)

학습 로그에 찍히는 값들이다. **나중에 전부 확인해서 이 섹션을 채운다.**

### SFT에서 나오는 메트릭 의미

| 메트릭 | 의미 | 볼 포인트 |
|--------|------|-----------|
| `loss` | completion-only cross-entropy | 내려가야 정상. 너무 빨리 0에 붙으면 과적합 의심 |
| `grad_norm` | 클리핑 전/후 gradient L2 norm | 폭주(폭발)면 학습 불안정 |
| `learning_rate` | 현재 step의 lr | warmup 후 cosine decay 되는지 |
| `entropy` | 출력 분포 엔트로피 | 과도하게 낮아지면 너무 확정적 |
| `num_tokens` | 누적 학습 토큰 수 | throughput/진척 확인 |
| `mean_token_accuracy` | next-token top-1 정확도 | loss와 반대로 올라가는지 |
| `epoch` | 현재 epoch 진행도 | 0→3 |
| step time | progress bar의 sec/it | 이번 런 ~78~83s/step |

### GRPO에서 나올 메트릭 (예정)

| 메트릭 | 의미 | 볼 포인트 |
|--------|------|-----------|
| `loss` | GRPO/policy loss | 안정적인지 |
| `reward` / `rewards/*/mean` | format reward 평균 | 올라가야 포맷 학습 중 |
| `reward_std` | 그룹 내 reward 분산 | 너무 0이면 학습 신호 약함 |
| `kl` / `beta` 관련 | ref policy 대비 KL | 너무 크면 정책이 과하게 흔들림 |
| `completions` 로그 | 생성 예시 | 포맷이 실제로 맞는지 육안 확인 |
| `grad_norm` | 정책 업데이트 크기 | 폭주 여부 |
| `learning_rate` | GRPO lr (`2e-6` 근처) | |

### SFT 중간 메트릭 스냅샷 (진행 중 자동 기록)

> 로깅 주기: `logging_steps=10`  
> 현재 학습: **진행 중** (아래는 로그에서 추출한 값)

| step | epoch | loss | grad_norm | lr | entropy | mean_token_acc | num_tokens |
|------|-------|------|-----------|----|---------|----------------|------------|
| 10 | 0.28 | 2.783 | 0.7761 | 1.996e-4 | 2.988 | 0.5252 | 4.55e5 |
| 20 | 0.57 | 1.781 | 0.4513 | 1.921e-4 | 1.651 | 0.6314 | 9.11e5 |
| 30 | 0.85 | 1.272 | 0.3111 | 1.759e-4 | 1.188 | 0.7108 | 1.38e6 |
| 40 | 1.11 | 1.025 | 0.317 | 1.526e-4 | 0.9849 | 0.753 | 1.80e6 |
| 50 | 1.40 | 0.8267 | 0.3085 | 1.244e-4 | 0.7883 | 0.7882 | 2.25e6 |
| 60 | 1.68 | 0.7396 | 0.3563 | 9.384e-5 | 0.7158 | 0.8061 | 2.71e6 |
| 70 | 1.96 | 0.6807 | 0.3549 | 6.388e-5 | 0.6574 | 0.820 | 3.17e6 |
| 80 | 2.23 | 0.603 | 0.3901 | 3.731e-5 | 0.5929 | 0.8371 | 3.59e6 |
| 90 | 2.51 | 0.5661 | 0.3452 | 1.664e-5 | 0.5491 | 0.8432 | 4.05e6 |
| 100 | 2.79 | 0.5636 | 0.3362 | 3.817e-6 | 0.5342 | 0.8446 | 4.51e6 |

관측 메모:
- loss `2.78 → 0.56`, token accuracy `0.53 → 0.85`로 단조 개선
- grad_norm 안정 (≈0.3~0.4), step time ≈ 75~83초, GPU당 VRAM ≈ 33.8GB

### SFT 최종 요약

- total steps: **108** (3 epochs)
- final `train_loss`: **1.044**
- final `mean_token_accuracy`: **0.8475**
- final `entropy`: **0.5514**
- wall-clock time: **8344s (~2h19m)**
- samples/sec: **0.407**
- adapter size: **~250MB**
- 특이사항: 저장 키에 `_checkpoint_wrapped_module` 포함 → merge 시 PEFT 키 정규화; `save_fsdp_adapter()`도 이후 저장용으로 수정

### GRPO 메트릭 표 (완료 후 채움)

| step | loss | reward_mean | reward_std | kl | grad_norm | lr | 메모 |
|------|------|-------------|------------|----|-----------|----|------|
|  |  |  |  |  |  |  |  |

### 메트릭 원본 로그 위치

- SFT stdout: torchrun 터미널 로그 / 추후 `outputs/sft_metrics.jsonl`
- GRPO stdout: 동일 / 추후 `outputs/grpo_metrics.jsonl`
- 이 문서에 최종 정리본을 남긴다

---

## 7-C. FSDP provenance 로그 (GRPO부터 강화)

SFT 본학습에는 이 로그가 약했고, **GRPO부터는 아래를 자동 저장**한다.

저장 위치: `outputs/provenance/`

| 파일/로그 | 내용 |
|-----------|------|
| `{stage}_rank{R}_env.jsonl` | `world_size`, `rank`, `local_rank`, device |
| `{stage}_rank{R}_fsdp_config.jsonl` | `FULL_SHARD`, wrap class, FSDP2 config |
| `{stage}_rank{R}_model_inspect.jsonl` | wrapped layer 수, trainable params |
| `{stage}_events.jsonl` | nvidia-smi 스냅샷 메타데이터 |
| `{stage}_nvidia_smi_{tag}_*.csv` | GPU별 VRAM/util |

찍히는 타이밍:
- before/after model load
- after trainer init
- `on_train_begin` / mid steps / `on_train_end`
- after adapter save

콘솔 예:
```text
[provenance] world_size=2 rank=0 local_rank=0 device=cuda:0
[provenance] FSDP strategy=FULL_SHARD version=2 wrap=['Gemma3DecoderLayer']
[provenance] GPU0 33800/40960 MiB util=100%
[provenance] GPU1 33750/40960 MiB util=100%
```

---

## 8. 다시 돌리는 방법

전체:
```bash
cd /workspace/fdsp_summary_format
bash run_pipeline.sh
```

단계별:
```bash
# SFT
/venv/main/bin/torchrun --standalone --nproc_per_node=2 train_sft.py

# SFT merge
/venv/main/bin/python merge_adapter.py --stage sft

# GRPO
/venv/main/bin/torchrun --standalone --nproc_per_node=2 train_grpo.py

# GRPO merge
/venv/main/bin/python merge_adapter.py --stage grpo
```

짧은 점검:
```bash
/venv/main/bin/torchrun --standalone --nproc_per_node=2 \
  train_sft.py --limit 32 --max-steps 1
```

필수 환경변수 (스크립트에 기본값으로 이미 넣어둠):
```bash
export NCCL_P2P_DISABLE=1
export NCCL_IB_DISABLE=1
export PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True
```

---

## 9. 한 줄 요약

기존 Unsloth 단일 GPU 파이프라인을 **같은 데이터/플로우로 FSDP 2-GPU LoRA**에 이식했고,  
이 머신에서 막히던 **NCCL P2P hang / tied embedding / full-state save** 문제를 해결한 뒤  
현재 **SFT·merge 완료**. GRPO는 초반 2 step만 돌리고 중단했다(과도한 wall-clock). FSDP 2GPU 검증 목표는 SFT로 달성.

핵심만 다시 말하면: **FSDP는 학습 알고리즘이 아니라 “큰 모델을 여러 GPU에 쪼개 올리는 방식”**이고,  
이번 작업은 그 방식 위에서 기존 SFT/GRPO 플로우를 재현한 것이다.
